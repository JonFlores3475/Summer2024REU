# Defines a module that has one function that can be used, which says hello to whomever called 
# the function (as long as they provide a name with them)

def greeting(name):
    print("Hello, " + name)